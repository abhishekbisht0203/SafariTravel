# Dependencies
/vendor/
/node_modules/

# Compiled assets (committed when deploying — optional, comment out if using CI build)
/theme/assets/dist/

# Vite dev flag
/theme/.vite-dev

# Environment secrets
.env
.env.local
.env.*.local

# WordPress uploads (media managed separately)
/uploads/

# WordPress core (managed via Docker image / Composer)
/wp/
/wordpress/

# Logs & caches
*.log
.phpunit.cache/
/tests/.phpunit.result.cache

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# PHP Composer lock — commit this for reproducible builds
# composer.lock  # keep this line commented; DO commit composer.lock

# Playwright
/playwright-report/
/test-results/
