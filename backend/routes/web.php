<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| Web routes
|--------------------------------------------------------------------------
|
| Safari Travel's website is WordPress. This application serves an API and
| nothing else, so there are deliberately no web routes here — WordPress owns
| every URL a visitor can reach.
|
| The file is kept because the framework's default routing configuration
| references it, and because it is the obvious place to add a route if a
| server-rendered page is ever genuinely needed.
|
*/
